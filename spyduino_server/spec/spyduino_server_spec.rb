require 'spec_helper'

describe SpyduinoServer do
  it 'has a version number' do
    expect(SpyduinoServer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
